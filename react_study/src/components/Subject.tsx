import React from "react";
import {render} from "react-dom";

const Subject = () =>{
    return (<header>
        <h1><a>
            onClick function(e){

            console.log();
            e.preventDefault();
        }
        </a></h1>
    </header>
    );
}
export default Subject